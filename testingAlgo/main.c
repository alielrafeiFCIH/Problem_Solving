#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    int i ,j,n,sum=0;
    int x = pow(2,1000);
    int y = pow(1000,2);
    printf("x = %d y = %d",x,y);
    return 0;
}
