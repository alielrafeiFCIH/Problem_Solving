#include <bits/stdc++.h>
using namespace std;

const int N = 105;
int adjMatrix[N][N];
int n , m;

int main()
{
    scanf("%d%d",&n,&m);
    int a , b , w;
    for(int i = 0 ; i < m ; i++){
        scanf("%d%d%d",&a,&b,&w);
        adjMatrix[a][b] = w;
    }

    return 0;
}

