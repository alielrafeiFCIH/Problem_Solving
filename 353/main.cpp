#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    set<string>st;
    while(cin>>s){
    for(int i=0;i<s.size();i++){
        for(int j=1;j+i<=s.length()-i;j++){
            string s1=s.substr(j,i);
            if(/func(s1))
        }
    }

    }
    return 0;
}
